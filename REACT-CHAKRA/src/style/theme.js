import { extendTheme } from "@chakra-ui/react"

export const customTheme = extendTheme({
  colors: {
    primary: "#f14668",
    secondary: "#fafafa",
    white: "#ffffff",
    href: "#3273dc",
    grey: "#9e9e9e"
  },
})